我想写一个Agent来做视频的发布，生成和评估，它包含如下功能，我提供一个思路，分为几个sub agent,一个文案 负责写文案，一个镜头师构思镜头和分镜，和一个裁判，它们都会接入同一个Agent，它们会一轮一轮的讨论形成最终方案，方案最多讨论三轮，方案是为了生成合适的prompt来生成视频。
视频生成有如下限制：视频一次只能生成五秒，且速度为20分钟一个视频，视频的风格需要保持一致，所以我们需要生成多个文案去做视频生成，这一点需要裁判特别注意。
视频生成我会提供一个API接口，在讨论结束后会调用API来依次生成视频片段，在我审核过后，选择对应的片段进行合成。
